<section id="about" class="py-20 bg-white">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 text-center">
        <p class="text-gold-600 font-semibold tracking-widest uppercase text-sm mb-3">{{ __('Our Story') }}</p>
        <h2 class="section-heading">{{ __('Made in Thuthibari, Loved by Thuthibari') }}</h2>
        <p class="text-maroon-500 mt-4 mb-14 max-w-2xl mx-auto">
            {{ __('What started as a small counter of fresh mithai has become the shop this town celebrates with — every festival, every wedding, every good news worth sharing.') }}
        </p>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-8">
            <div class="bg-cream rounded-2xl px-6 py-8 border border-gold-300/40">
                <span class="text-5xl">🥇</span>
                <h3 class="font-display font-bold text-lg text-maroon-900 mt-4">{{ __('No. 1 in Thuthibari') }}</h3>
                <p class="text-maroon-500 text-sm mt-2">{{ __('The most trusted name in town, earned one happy customer at a time.') }}</p>
            </div>
            <div class="bg-cream rounded-2xl px-6 py-8 border border-gold-300/40">
                <span class="text-5xl">🐄</span>
                <h3 class="font-display font-bold text-lg text-maroon-900 mt-4">{{ __('100% Desi Ghee') }}</h3>
                <p class="text-maroon-500 text-sm mt-2">{{ __('Pure ingredients, no shortcuts — the way mithai was always meant to be.') }}</p>
            </div>
            <div class="bg-cream rounded-2xl px-6 py-8 border border-gold-300/40">
                <span class="text-5xl">🌅</span>
                <h3 class="font-display font-bold text-lg text-maroon-900 mt-4">{{ __('Fresh at Dawn') }}</h3>
                <p class="text-maroon-500 text-sm mt-2">{{ __('Made every morning, sold the same day. Never stored, never stale.') }}</p>
            </div>
        </div>
    </div>
</section>
